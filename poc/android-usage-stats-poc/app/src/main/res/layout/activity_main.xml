<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/screen_background"
    android:padding="@dimen/screen_padding">

    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        app:cardBackgroundColor="@color/surface_primary"
        app:cardCornerRadius="@dimen/launch_card_corner_radius"
        app:cardElevation="@dimen/launch_card_elevation"
        app:strokeColor="@color/surface_stroke"
        app:strokeWidth="@dimen/launch_card_stroke_width">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical"
            android:paddingHorizontal="@dimen/launch_card_horizontal_padding"
            android:paddingVertical="@dimen/launch_card_vertical_padding">

            <TextView
                android:layout_width="@dimen/brand_mark_size"
                android:layout_height="@dimen/brand_mark_size"
                android:background="@drawable/brand_mark_background"
                android:fontFamily="sans-serif-medium"
                android:gravity="center"
                android:text="@string/brand_mark"
                android:textColor="@color/on_brand"
                android:textSize="@dimen/brand_mark_text_size"
                android:textStyle="bold" />

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/titleText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/title_margin_top"
                android:gravity="center"
                android:text="@string/app_title"
                android:textColor="@color/text_primary"
                android:textSize="@dimen/title_text_size"
                android:textStyle="bold" />

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/statusText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/status_margin_top"
                android:gravity="center"
                android:text="@string/app_loaded_status"
                android:textColor="@color/status_success"
                android:textSize="@dimen/status_text_size" />

        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>

</FrameLayout>
